# Vercel Postgres connection string
# When you connect a Vercel Postgres database via the Vercel dashboard,
# this environment variable is automatically injected.
# For local development, copy this file to .env.local and fill in the value.
POSTGRES_URL=
